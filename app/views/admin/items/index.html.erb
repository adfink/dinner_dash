<h1>menu items</h1>

<ul>
  <% @items.each do |item| %>
    <li>  <%= link_to item.title, admin_item_path(item) %> | <%= link_to "edit", edit_admin_item_path(item) %></li>
  <% end %>
</ul>
<h4><%= link_to "add an item", new_admin_item_path %></h4>

<h6><%= link_to "back to admin page", admin_user_path(current_user) %></h6>
