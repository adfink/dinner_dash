<h1>a little bit of information about your order...</h1>
<strong>order status:</strong> <%= @order.status %><br>
<strong>date order was submitted:</strong> <%= @order.created_at %>
<table class="table-responsive">
  <th>

  </th>
  <% @order.items.each do |item| %>
  <tr class="active">
    <td>
      <%= link_to "#{item.title}", item_path(item) %>
    </td>
    <td>

    </td>
  </tr>
  <% end %>
</table>

<!-- need to write test for table, need to build link  -->
